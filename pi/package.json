{
  "name": "cookbook-pi-extensions",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --experimental-strip-types --test \"extensions/**/*.test.ts\"",
    "typecheck": "tsc --project tsconfig.json",
    "check-version": "node --experimental-strip-types scripts/check-pi-version.ts",
    "check": "pnpm run check-version && pnpm run typecheck && pnpm test"
  },
  "devDependencies": {
    "@earendil-works/pi-ai": "0.86.1",
    "@earendil-works/pi-coding-agent": "0.86.1",
    "@earendil-works/pi-tui": "0.86.1",
    "@types/node": "24.10.13",
    "typebox": "1.3.7",
    "typescript": "5.9.3"
  }
}
